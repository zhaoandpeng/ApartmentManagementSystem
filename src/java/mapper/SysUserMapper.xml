<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="cn.com.mrs.zhao.dao.SysUserMapper">  
    <select id="getLoginResult" parameterType="String" resultType="SysUser">  
       select * from sys_user where username=#{1} and password=#{0}  
    </select>
</mapper>